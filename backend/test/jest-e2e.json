{
  "moduleFileExtensions": [
    "js",
    "json",
    "ts"
  ],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": "ts-jest"
  },
  "testTimeout": 30000,
  "_comment": "Suites share one database and each rebuilds the schema, so they must not overlap. The test:e2e script passes --runInBand; maxWorkers alone limits the worker pool but still allows one suite to start while another tears down."
}
