node_modules
dist
.next
.env.local
*.log
.DS_Store
coverage

# Keep committed backend/.env for local defaults; ignore other env files.
.env
!backend/.env
!backend/.env.example


# MikroORM writes a schema snapshot per database; only the dev one is shared.
backend/src/migrations/.snapshot-*_test.json

# Next.js incremental typecheck cache
frontend/tsconfig.tsbuildinfo
